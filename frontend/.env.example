# Vite Environment Variables

# For production builds, you might need to specify the full URL of your backend API.
# For local development, this is NOT needed because the Vite proxy is used.
# Example: VITE_API_URL=https://api.yourproductiondomain.com